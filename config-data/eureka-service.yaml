server:
  port: 8761

#INDICAMOS QUE NO REGISTRE A ESTE SERVICIO, PUES ES EL SERVIDOR
eureka:
 client:
   fetch-registry: false
   register-with-eureka: false
   service-url:
    default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
