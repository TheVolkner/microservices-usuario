spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  application:
    name: carro-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/auth/realms/tutorial-realm