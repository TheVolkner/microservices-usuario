spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  application:
    name: auth-service
jwt:
  secret: TechPantherKey3014
admin-paths:
  paths:
    -
      uri: "/api/usuarios/agregar"
      method: "POST"
    -
      uri: "/api/usuarios/carro/agregar/{1}{1-9}+{0-9}"
      method: "POST"
